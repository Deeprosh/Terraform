# Conditional operators  
- When we want to use if conditions based on some conditions we will use these conditional operators.
### Syntax:  
condition ? true_val : false_val  

# Built-in Finctions  
- There are some built-in functions in terraform which we can use by using keywords.
Example: length, map, conact, element etc.,
